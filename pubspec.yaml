name: moneta
description: "Moneta — a local-first personal finance app, built design-system-first from Figma."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.2

dependencies:
  equatable: ^2.1.0
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.4.2
  go_router: ^17.5.0
  intl: ^0.20.3
  meta: ^1.18.0
  path: ^1.9.1
  sqflite: ^2.4.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.5
  sqflite_common_ffi: ^2.4.2+1
  very_good_analysis: ^10.3.0

flutter:
  uses-material-design: true
